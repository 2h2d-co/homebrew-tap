name: CI
on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
permissions: {}
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  check:
    name: Check formulae
    runs-on: macos-15
    permissions:
      actions: read # Required by brew test-bot to inspect workflow artifacts.
      checks: read # Required by brew test-bot to inspect check runs.
      contents: read # Required to check out and inspect the tap.
      pull-requests: read # Required by brew test-bot to inspect changed formulae.
    steps:
      - name: Set up Homebrew
        id: set-up-homebrew
        uses: Homebrew/actions/setup-homebrew@c8707045ccae42888fe98e86f2ee8938bc7cc193 # 2026.08.10.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Cache Homebrew Bundler RubyGems
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.set-up-homebrew.outputs.gems-path }}
          key: ${{ runner.os }}-rubygems-${{ steps.set-up-homebrew.outputs.gems-hash }}
          restore-keys: ${{ runner.os }}-rubygems-
      - uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0
        with:
          version: "2026.8.2"
          install_args: --locked
          cache: false
      - name: Run shared checks
        run: hk check --all --check
      - name: Prepare Homebrew test environment
        run: |
          brew test-bot --only-cleanup-before
          brew test-bot --only-setup
      - name: Check tap syntax
        run: brew test-bot --only-tap-syntax
      - name: Audit formulae
        env:
          TAP_NAME: ${{ steps.set-up-homebrew.outputs.tap-name }}
        run: brew audit --strict --tap="$TAP_NAME"
      - name: Test changed formulae
        if: github.event_name == 'pull_request'
        run: brew test-bot --only-formulae
